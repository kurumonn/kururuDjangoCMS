# Django migration専用role。Web / worker / maintenanceへは渡さない。
#
#   cp .env.db-migration.example .env.db-migration
#   chmod 0600 .env.db-migration
#
# DB・schema・テーブルの所有者だが、superuser・DB作成・role作成は不可。
POSTGRES_MIGRATION_USER=kururucms_migrator
POSTGRES_MIGRATION_PASSWORD=ここに生成したmigration専用の値を貼る
