# PostgreSQL公式イメージとDBロールprovisionだけが読む管理者資格情報。
#
#   cp .env.db-admin.example .env.db-admin
#   chmod 0600 .env.db-admin
#
# .env.db-adminはGit管理外であり、web / worker / maintenanceへは渡さない。
POSTGRES_USER=postgres
POSTGRES_PASSWORD=ここに生成したDB管理専用の値を貼る
