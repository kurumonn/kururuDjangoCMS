# 連載記事

第1部 **10日で作る Django CMS ― 基礎からワンタイムコード・パスキー認証まで**
第2部 **10日で学ぶ Django 本番デプロイ ― Linux・Nginx・秘密鍵・Let's Encrypt・セキュリティ**

各記事は次の13項目で統一しています。

1. 今日の結論
2. 今日の完成画面
3. 今日変更するファイル
4. 完成コード
5. コードの意味
6. 内部で起きていること
7. コマンドの説明
8. よくあるエラー
9. 動作確認
10. セキュリティ上の注意
11. 今日の復習問題
12. Git の差分
13. 次回予告

「8. よくあるエラー」は想像で書いていません。
[`docs/errors/`](../errors/) に記録した、**実際に手が止まったもの**だけを載せています。

## 環境別の手順の書き方

OS やディストリビューションでコマンドが変わるところは、
**全部を並べたうえで、読者に選ばせます**。

原稿にはこう書きます。

```html
<div class="env-block env-ubuntu">

**Ubuntu 24.04 LTS**

```bash
sudo apt update
```

</div>

<div class="env-block env-rhel env-alma">

**Oracle Linux / AlmaLinux**

```bash
sudo dnf check-update
```

</div>
```

使えるキーは次のとおりです。

| キー | 表示名 | 軸 |
| --- | --- | --- |
| `env-ubuntu` | Ubuntu 24.04 LTS | サーバーの環境 |
| `env-rhel` | Oracle Linux / RHEL 系 | サーバーの環境 |
| `env-alma` | AlmaLinux / Rocky Linux | サーバーの環境 |
| `env-windows` | Windows (PowerShell) | 手元の端末 |
| `env-macos` | macOS | 手元の端末 |
| `env-linux` | Linux | 手元の端末 |

**軸が違うものは別のプルダウンになります。**
1つの記事に「サーバーの OS」と「手元の端末の OS」が両方出てくる場合
（11日目がその例）、プルダウンが2つ並びます。

1つにまとめると壊れます。手元の端末で Linux を選んだ瞬間、
サーバー側の Ubuntu / RHEL の手順がどれにも当てはまらなくなって
全部消えるためです。

コマンドが同じ環境は、1つのブロックに複数のキーを書いてまとめます
（上の `env-rhel env-alma` がその例）。

- **GitHub 上では全環境がそのまま表示されます。** 読者は差分を見比べられます。
- **ブログでは記事の先頭にプルダウンが出て、選んだ環境だけが表示されます。**
  選択はブラウザーに記憶され、連載の他の記事にも引き継がれます。
- JavaScript が動かない場合は全環境が表示されます。
  隠すのは JS の役目にしてあるので、「選ばないと何も読めない」状態にはなりません。

`div` の直後と `</div>` の直前には**空行が必要**です。
無いと Markdown が中身を変換せず、記法が生のまま表示されます。

## 原稿のきまり

`div` の直後と `</div>` の直前には空行が必要です（上記）。もう1つあります。

**引用（`>`）の中にコードフェンスを書かないでください。**

Python-Markdown の `fenced_code` は前処理として行単位で走り、
フェンスの判定を行頭から行うため `> ```bash` には反応しません。
エラーは出ないまま、コードブロックが1つの `<code>` に潰れて公開されます。

補足の中にコードを置きたい場合は、引用をやめて `####` の見出しにします。

どちらの決まりも、原稿を検査するテストで固定してあります
（`diary/tests_djangocms_series.py` の `ManuscriptRuleTests`）。

## 画像について

「2. 今日の完成画面」の画像は、このリポジトリには入れていません。
代わりに次のマーカーを置き、ブログへ投稿するときだけ実際の画像へ差し替えます。

```text
<!-- screenshot: day-08-login.png | ログイン画面 -->
```

HTML コメントなので GitHub 上では表示されません。
どの画面のことかは、マーカーの直前の文が説明しています。
画像が必要になったら `python tools/capture_screenshots.py` で撮り直せます。

## 記事一覧

### 下書き

### 第1部：CMS を作る

| 日 | 記事 | タグ |
| ---: | --- | --- |
| 1 | [Django CMS 開発を始めよう――環境構築からカスタムユーザーまで](day-01.md) | `day-01` |
| 2 | [Django モデル入門――記事・カテゴリ・タグをデータベースへ保存する](day-02.md) | `day-02` |
| 3 | [Django CRUD 完全入門――記事一覧から投稿・編集・削除まで作る](day-03.md) | `day-03` |
| 4 | [Django CMS を実用化――画像投稿・コメント・サイト内検索を作る](day-04.md) | `day-04` |
| 5 | [WordPress 級の記事管理――下書き・予約投稿・リビジョンを実装する](day-05.md) | `day-05` |
| 6 | [Django CMS の SEO 対策――OGP・構造化データ・RSS・サイトマップ](day-06.md) | `day-06` |
| 7 | [Django で管理画面を自作――ダッシュボードとブロックエディター](day-07.md) | `day-07` |
| 8 | [django-allauth 完全入門――メール認証とワンタイムコードログイン](day-08.md) | `day-08` |
| 9 | [Django でパスキー認証――TOTP・WebAuthn・復旧方法まで実装](day-09.md) | `day-09` |
| 10 | [Django CMS 完成――テスト・Docker・PostgreSQL・Redis・本番設定](day-10.md) | `day-10` |

### 第2部：本番へデプロイする

| 日 | 記事 | タグ |
| ---: | --- | --- |
| 11 | [Linux サーバー初期設定――借りた直後にやること・やってはいけない順序](day-11.md) | `day-11` |
| 12 | [SSH を鍵認証だけにする――締め出されない手順と、戻れる作り方](day-12.md) | `day-12` |

## 読み方

記事を読むだけでも進められますが、手元で動かすと理解が早いです。

```bash
git clone https://github.com/kurumonn/DjangoCMS.git
```

その日の状態へ移動します。

```bash
git checkout day-03
```

前日から何が変わったかを見ます。

```bash
git diff day-02 day-03
```
